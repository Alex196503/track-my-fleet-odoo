For local development and testing automated email reports, a Docker-based Mailpit SMTP server is configured in `docker-compose.yaml`.

### Setup & Access
1. Start the Mailpit service:
   ```bash
   docker compose up -d
2. Access the Mailpit Web UI to view intercepted emails:
   - http://localhost:8025/view/0MH42bOMTbQgZacpjPqCs5
